"use strict";
const getItemsString = function (array) {
  "use strict";
  for (const item of getItemsString) {
    let n = 0;
    let result = `${n + 1} - ${item}\n `;
  }
  return result;
};
console.log(getItemsString(["Mango", "Poly", "Ajax", "Lux", "Jay", "Kong"]));
